# Offline install requirements for Python 3.10..3.13 (download wheels once, then install without internet).
#
# Versions are PINNED on purpose. We hit a real client outage when plotly v6
# silently removed `mapbox.fitbounds` and a streamlit minor changed slider
# validation. Pinning here + DOWNLOAD_WHEELS.py --all-supported guarantees the
# offline install matches what we tested.
pandas==2.2.3
numpy==1.26.4
plotly==5.24.1
streamlit==1.39.0
requests==2.32.3
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2
